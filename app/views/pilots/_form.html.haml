.row
  .col-sm-12
    = panel do
      = panel_body do
        = simple_form_for @pilot, wrapper: :vertical_form do |f|
          .row
            .col-sm-6
              .row
                .col-sm-12
                  = f.input :email, required: false, autofocus: true
              .row
                .col-sm-6
                  = f.input :first_name, required: false
                .col-sm-6
                  = f.input :last_name,  required: false
            .col-sm-3
              = f.input :avatar
            - if @pilot.avatar.file
              .col-sm-3
                = avatar_image_tag @pilot.avatar, :thumb, class: 'lazy'
                = checkbox_tag f, :remove_avatar

          %hr
          = f.submit class: 'btn btn-primary'
