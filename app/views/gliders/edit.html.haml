- breadcrumb :glider, @glider, true

- content_for :title, t('pages.gliders.titles.edit', immatriculation: @glider.immatriculation)
- content_for :actions do
  = show_action_link_to @glider

= render 'form'
