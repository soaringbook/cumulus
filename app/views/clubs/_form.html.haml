.row
  .col-sm-6
    = panel do
      = panel_body do
        = simple_form_for @club, wrapper: :vertical_form do |f|
          = f.input :short_name, required: false, autofocus: true
          = date_selection_tag f, :active_until

          %hr
          = f.submit class: 'btn btn-primary'
          - unless @club.new_record?

            .pull-right= link_to t('pages.clubs.labels.invite.button'), new_club_pilot_path(@club) , data: { modal: true }, class: 'btn btn-white'
